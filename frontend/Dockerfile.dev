FROM node:25-alpine
WORKDIR /app
# source is bind-mounted by docker-compose.dev.yml; install runs at container start
# so it always matches whatever package.json is currently on disk.
CMD ["sh", "-c", "npm install && npm run dev -- --host"]
